import { createMuiTheme } from '@material-ui/core/styles';

const HeadwayTheme = createMuiTheme({
    palette: {
        primary: {
            main: '#12c97a',
            contrastText: '#fff',
        },
        secondary: {
            main: '#ccc',
        },
    },
});

export default HeadwayTheme;